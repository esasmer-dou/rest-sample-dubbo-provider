# rest-sample-dubbo-provider 0.6.4

This patch aligns the provider build with `rust-java-rest:4.5.0` while retaining
`java-rust-dubbo:0.7.2` and the existing provider runtime.

Provider services, database behavior, profiles, concurrency limits, and ZooKeeper options are
unchanged.
