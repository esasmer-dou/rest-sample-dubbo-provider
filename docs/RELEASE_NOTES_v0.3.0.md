# rest-sample-dubbo-provider v0.3.0

This sample aligns with `java-rust-dubbo:0.4.0` and the `0.3.0` consumer sample.

- Preserves static and ZooKeeper provider modes.
- Preserves PostgreSQL/HikariCP query-only and full provider shapes.
- Keeps interface-level and method-level execution limits explicit.
- Refreshes Docker/Jlink images with the current low-memory OpenJ9 options.
